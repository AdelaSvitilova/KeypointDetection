# Basic configuration, can be overwritten by other files

experiment:
  name: atlas_test
  continue_from: epoch_10.pt  # null or checkpoint (epoch_10.pt/best.pt)

loss:
  name: MSE

metrics:
  names: ["PCK"]

train:
  batch_size: 16
  lr: 0.01
  epochs: 100
  save_every_epoch: 10
  device: null #"cuda:4"