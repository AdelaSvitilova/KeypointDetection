keypoints: heatmaps

model:
  name: test_model_heatmap_pytorch #test_model_heatmap_pytorch #stacked_hourglass_pytorch #test_model_heatmap_keras
  backend: pytorch
  params:
    input_channels: 3
    num_keypoints: 17

dataset:
  name: random_heatmaps
  split: ["train2017", "val2017"]
  params:
    num_keypoints: 17

loss:
  name: MSE

metrics:
  names: ["PCK"]

train:
  batch_size: 2
  lr: 0.01
  epochs: 6
  val_every_epochs: 2